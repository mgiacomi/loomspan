import { type FormEvent, useEffect, useRef, useState } from "react";import { useTarget } from "./TargetProvider";const labels: Record<string, string> = {  NOT_APPLICABLE: "Not applicable",  NOT_CHECKED: "Not checked",  NOT_ESTABLISHED: "Not established",  NONE: "None",  SELECTED: "Selected",  UNKNOWN: "Unknown",  REACHABLE: "Reachable",  UNAVAILABLE: "Unavailable",  REQUIRED: "Required",  ESTABLISHED: "Established",  BLOCKED: "Blocked",  COMPATIBLE: "Compatible",  INCOMPATIBLE: "Incompatible",  AVAILABLE: "Available",};const transportGuidance: Record<string, string> = {  dns: "Check the target hostname and local DNS configuration.",  connection: "Check that the application is running and reachable at the selected address.",  timeout: "Check application responsiveness and the configured Console network timeouts.",  tls_untrusted_issuer: "Use a certificate trusted by the operating system or configure its issuing CA in the Console profile.",  tls_hostname_mismatch: "Use a target hostname covered by the application certificate.",  tls_expired: "Renew the application certificate before reconnecting.",  tls_not_yet_valid: "Check system clocks and the certificate activation time.",  tls_handshake: "Check the application's TLS configuration and supported protocol versions.",  redirect: "Enter the final application address directly; the Console does not follow redirects.",  namespace_not_found: "Check the target context path; the Loomspan observability namespace was not found.",  upstream_server: "Check the application logs, then recheck the target.",  upstream_protocol: "Confirm that the selected address serves the compatible Loomspan observability API.",};export function Overview() {  const { target, error, scopeGeneration, connect, credential, recheck } = useTarget();  const [address, setAddress] = useState("");  const [key, setKey] = useState("");  const [busy, setBusy] = useState(false);  const [replacing, setReplacing] = useState(false);  const heading = useRef<HTMLHeadingElement>(null);  const selected = target.status.targetSelection === "SELECTED";  const guidance = error?.details?.transportCategory    ? transportGuidance[error.details.transportCategory] ??      "Check the selected target's network and protocol configuration."    : error?.code === "TARGET_ACCESS_BLOCKED"      ? "Check application-host and reverse-proxy access rules before replacing the key."      : undefined;  useEffect(() => {    if (scopeGeneration > 0) heading.current?.focus();  }, [scopeGeneration]);  const submit = async (event: FormEvent) => {    event.preventDefault();    const submittedKey = key;    setBusy(true);    try {      if (selected && !replacing) await credential(submittedKey);      else await connect(address, submittedKey);    } catch {      heading.current?.focus();    } finally {      setKey("");      setBusy(false);    }  };  return (    <section aria-labelledby="overview-title" className="overview-card">      <p className="eyebrow">Selected target</p>      <h2 id="overview-title" ref={heading} tabIndex={-1}>Overview</h2>      {!selected ? (        <p>Select the Loomspan application this Console should observe.</p>      ) : (        <p>          <strong>{target.address}</strong>          {target.unencrypted && (            <span className="unencrypted">Unencrypted ΓÇö the application key and observability data travel without network encryption.</span>          )}        </p>      )}      {selected && (        <p className="target-warning">Trace errors can contain application exception messages, causes, suppressed exceptions, and stack text. Loomspan does not secret-scan or redact this application diagnostic content.</p>      )}      <dl className="status-grid" aria-label="Independent target facts">        <Fact name="Selection" value={target.status.targetSelection} />        <Fact name="Connection" value={target.status.targetConnection} />        <Fact name="Authentication" value={target.status.targetAuthentication} />        <Fact name="Java/Go compatibility" value={target.status.javaGoCompatibility} />        <Fact name="Runtime identity" value={target.status.runtimeIdentity} />        <Fact name="Instance ID" value={target.status.instanceId ?? "Not established"} />        <Fact name="Live monitoring" value={target.status.liveMonitoring} />        <Fact name="Observed at" value={target.status.observedAt} />      </dl>      {error && (        <div className="target-error" role="alert">          <strong>{error.message}</strong>          {guidance && <p>{guidance}</p>}          {error.code === "INCOMPATIBLE_TARGET" && (            <p>Expected {error.details?.expectedCompatibilityVersion}; observed {error.details?.observedCompatibilityVersion}.</p>          )}        </div>      )}      <form onSubmit={submit} className="target-form">        {(!selected || replacing) && (          <label>            Target address            <input type="url" required maxLength={2048} value={address} onChange={(event) => setAddress(event.target.value)} />          </label>        )}        {replacing && (          <p role="status">            Replacing the target clears all data associated with the current target scope.          </p>        )}        <label>          Application key          <input type="password" required minLength={32} maxLength={512} autoComplete="off" value={key} onChange={(event) => setKey(event.target.value)} />        </label>        <button type="submit" disabled={busy}>          {replacing ? "Replace target" : selected ? "Supply or replace key" : "Connect"}        </button>      </form>      {selected && (        <>          {replacing ? (            <button type="button" disabled={busy} onClick={() => {              setReplacing(false);              setAddress("");              setKey("");            }}>              Cancel target replacement            </button>          ) : (            <button type="button" disabled={busy} onClick={() => setReplacing(true)}>              Change target            </button>          )}          <button type="button" disabled={busy || replacing} onClick={() => void recheck().catch(() => heading.current?.focus())}>            Recheck target          </button>        </>      )}    </section>  );}function Fact({ name, value }: { name: string; value: string }) {  return (    <div>      <dt>{name}</dt>      <dd>{labels[value] ?? value}</dd>    </div>  );}
